# location - ~/.config/bat/config
# auto:system reads the macOS appearance directly, so it works through tmux and
# when piped (fzf previews) rather than sniffing terminal colors.
--theme=auto:system
--theme-light="OneHalfLight"
--theme-dark="OneHalfDark"
